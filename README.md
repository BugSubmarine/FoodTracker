# FoodTracker
An application to help plan meals. Set goals for a diet (Fibre, Protein, Carb, Fat), and build meals. This will be a MERN-stack project. 

make sure to run npm install to install node packages

both the .env file and node_modules directory are not included...

use npm run dev to run the backend
use npm start to run the frontend